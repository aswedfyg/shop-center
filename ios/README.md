# iOS Test Package

This folder is the landing zone for the macOS iOS Appium project.

- Keep XCUITest/WebDriverAgent execution on macOS.
- Share data, report helpers, and backend expectations through `common`.
- Keep iOS locators and platform recovery logic inside `ios`.
- Do not put iOS-specific Xcode or WDA setup inside Android scripts.

